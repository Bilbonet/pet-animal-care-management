<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--customer invoice wizard form-->
    <record id="view_customer_invoice" model="ir.ui.view">
        <field name="name">customer.invoice.form</field>
        <field name="model">customer.invoice</field>
        <field name="arch" type="xml">
            <form string ="Customer Penalty">
                <group> 
                    <group> 
                        <field name="start_date" required='True'/>
                    </group>
                    <group>  
	                <field name="end_date" required='True'/>
                    </group>
                </group> 
	    	<footer>
	            <button string='Print' name="print_report" type='object' class='btn-primary'/>
		    <button string='Cancel' class='btn-default' special="cancel"/>	
	    	</footer>
	    </form>		
        </field>
    </record>
    <!--Action for wizard form -->
    <record id="action_customer_invoice_wizard" model="ir.actions.act_window">
        <field name="name">Customer Invoice</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">customer.invoice</field>
        <field name="view_type">tree</field>
        <field name="view_mode">form,tree</field>
        <field name="target">new</field>
        <field name="view_id" ref="view_customer_invoice"/>	
    </record>
</odoo>
